AI 정규 스터디 1주차 강의를 들었다. 복수 전공으로 컴퓨터 공학으로 진입한 것이라서 부담이 조금 있었다. 하지만 1주차인지라 아주 기본적인 개념만 배운 것 같아 어렵진 않았다. 먼저 도미 데이터를 길이와 무게로 배열처럼 나타내었다. 파이썬은 배우지 않았고 학교 커리큘럼으로 c언어와 c++언어만 배운 상태인 입장에서 파이썬의 변수 선언은 매우 간단했다. 자료형 없이 선언할 수 있다는 것이 놀라웠다. 그저 변수 = [a, b, c] 등의 형식으로 입력하면 그것이 곧 배열이 된다. 다음으로 import A as B를 통해 라이브러리를 간략화할 수 있는 걸 배웠다. matplotlib 라이브러리를 통해 데이터를 시각화까지 했다. scatter(a,b)를 통해 x, y축 데이터를 설정하고 xlabel(a), ylabel(b)를 통해 각 축의 이름까지 설정했다. 이는 생소하긴 했지만 c와 c++에서는 경험해보지 못한 데이터 시각화를 하니 AI에 한층 더 다가간듯한 느낌이 들었다. 도미 데이터와 빙어 데이터의 길이와 무게를 더하는 식도 간단했다. 그저 변수+변수로 새로운 변수에 배열의 덧셈을 한 것이다. 왜 이때까지 파이썬을 경험하지 않았을까 하는 아쉬움도 든다. fish_data = [[l, w] for l, w in zip(length, weight)] 이 식은 이해하는 데 어려웠다. 결과값을 통해 어떤 기능을 하는지는 인지했지만 for? in? c언어에서 배운 for이랑 문법이 달랐고 zip(a,b)도 생소했다.
다음으로 kn.predict로 새로운 fish 데이터 3개를 어떤 생선인지 예측해보는 과제를 했다. 먼저 [30, 600]의 생선은 도미로 예측했다. 이는 그래프를 통해 봤을 때도 도미로 보였다. 다음은 [12, 10]이다. 이 역시 빙어로 예측이 충분히 가능했다. 하지만 [25, 150]의 데이터는 그래프 상에서 도미로 보였으나 빙어로 예측했다. 마지막 데이터가 내가 예측한 결과와 knn분류기가 예측한 결과는 달랐다. 